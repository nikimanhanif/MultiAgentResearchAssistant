"""Base agent interface and protocol definitions.

This module defines the base interface/protocol that all agents should follow.
Future implementation will include proper protocol/ABC definitions.

Future implementation:
- Protocol/ABC for BaseAgent
- async def process(query: str) -> str
- async def validate_input(query: str) -> bool
- Agent types enum (SCOPE, RESEARCH, REPORT, etc.)
"""

